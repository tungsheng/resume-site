services:
  app:
    image: "${IMAGE_REPOSITORY:?IMAGE_REPOSITORY is required}:${IMAGE_TAG:-latest}"
    pull_policy: always
    ports:
      - "127.0.0.1:3000:3000"
    environment:
      NODE_ENV: production
      TRUST_PROXY: "1"
      PDF_MAX_CONCURRENT_EXPORTS: "${PDF_MAX_CONCURRENT_EXPORTS:-2}"
      PDF_RENDER_TIMEOUT_MS: "${PDF_RENDER_TIMEOUT_MS:-30000}"
    restart: always
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: "3"
